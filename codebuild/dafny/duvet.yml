version: 0.2
phases:
  install:
    commands:
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
      - rustup install stable -y
      - ./aws-encryption-sdk-specification/util/install-duvet
  build:
    commands:
      - ./duvet_report.sh
