version: 0.2

batch:
  fast-fail: true
  build-graph:
    - identifier: sign
      buildspec: aws-encryption-sdk-net-formally-verified/codebuild/release/sign.yml
      env:
        variables:
          UNIQUE_ID: $UNIQUE_ID
    - identifier: release_staging 
      buildspec: aws-encryption-sdk-net-formally-verified/codebuild/release/release-staging.yml
      env:
        variables:
          UNIQUE_ID: $UNIQUE_ID
      depend-on:
        - sign
